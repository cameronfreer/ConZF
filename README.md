# ConZF (branch `no-em-bounds`)

This branch removes excluded middle from the proof of `Con ZF` as far as it can be removed. The
whole development is carried out over *stable* relations (bisimulation and membership with
`¬¬∃` at every level, the Gödel–Gentzen reading), so that all of the set theory, including the
theory of ordinals, is proved classically without assuming excluded middle. What is left is one
hypothesis, instancewise: the output ranks of every definable functional relation over the model
are not not bounded by an ordinal.

```lean
def RankBounded (ψ : Fml) (e : Nat → PSet) (a : PSet) : Prop :=
  ¬¬∃ κ, IsOrd κ ∧ ∀ x y, x ∈ a → HG y → Sat HG ψ (Env.cons x (Env.cons y e)) → rank y ∈ κ
def BoundHyp : Prop := ∀ ψ e, (∀ i, HG (e i)) → ∀ a, HG a → (functional) → RankBounded ψ e a

theorem PSet.con_ZF_of_bounds (hb : BoundHyp) : Con ZF
-- 'PSet.con_ZF_of_bounds' does not depend on any axioms
```

Only one canonical form of definition needs bounds (`EnvFml.lean`): the *envelope* of a
formula, the set of its witnesses of least rank, or empty. Envelope instances are total and
functional, and `EnvBoundHyp`, rank bounds for envelope instances alone, gives `BoundHyp`
(`boundHyp_of_envBoundHyp`, `con_ZF_of_envBoundHyp`).

The bound is consumed by one Separation (`CofinalCut.lean`): the cut `{ζ ∈ κ | some output rank
reaches ζ}` is hereditarily good for *every* ordinal cap `κ`, and once `κ` is a bound the level
`V` of the cut collects the outputs. No materializing recursion appears in the consumer.

Two producers of the bounds are proved, both from accessibility, which is not a stable
proposition:

```lean
def AccHyp : Prop := ∀ τ : Path → PSet → Prop, Desc τ → ¬¬Acc (Rel τ) []
def PointwiseHGAcc : Prop := ∀ η, Cls ISat η → ¬¬Acc (· ∈ ·) η

theorem PSet.con_ZF (hacc : AccHyp) : Con ZF
-- 'PSet.con_ZF' does not depend on any axioms
theorem PSet.con_ZF_of_pointwise (hacc : PointwiseHGAcc) : Con ZF
theorem PSet.con_ZF_of_mem_wf (h : ¬¬∀ x : PSet, Acc (· ∈ ·) x) : Con ZF
theorem PSet.con_ZF_of_not_not_em (h : ¬¬∀ p : Prop, p ∨ ¬p) : Con ZF
```

`AccHyp` implies `PointwiseHGAcc` (`pointwiseHGAcc_of_accHyp`), whose quantifier is outside the
double negation; the pointwise producer guards the recursion at each component separately
(`Pointwise.lean`) instead of assuming the root of the glued tree accessible.

`Desc τ` says that the target of a child is an element of the target of its parent. The paths
are needed as the carrier of the recursion; the accessibility of a path follows from the
accessibility of its target under membership (`acc_of_desc`).

The converse is also proved:

```lean
theorem PSet.accHyp_iff_not_not_mem_wf :
  AccHyp ↔ ¬¬∀ x : PSet, Acc (· ∈ ·) x
```

This is uniform double-negated accessibility; stable membership induction is
already available. `Acc.lean` proves accessibility of finite ordinals and closure
under Power Set. `Markov.lean` shows that accessibility of `omega` implies Markov's
principle, with witnesses in Type when given a decision procedure. `AccHyp` implies
uniform double-negated `PropMarkov` and `Markov` by this argument. `PropMarkov`
uses decisions in `Prop` (`P n ∨ ¬P n`); `Markov` uses `Decidable (P n)`.
The hypotheses remain unproved; see [the accessibility notes](doc/accessibility.md), which
also record what the bounds would need (`NativeBound.lean`) and a classical test model showing
that the set-theoretic consequences extracted so far cannot force them.

Compared to `main`: no case distinction between zero, successor and limit ordinals in the
definability rule (`Reach.lean`); a single model, the sets of hereditarily good rank
(`Model.lean`); no `dichotomy`.

## Build

    lake build

(Lean toolchain as in `lean-toolchain`; no dependencies.)

## Files

| File | Content |
|---|---|
| `ConZF/Logic.lean` | stable propositions; classical reasoning for stable goals |
| `ConZF/PSet.lean` | sets as trees with stable bisimulation and membership, induction for stable predicates, operations, guards |
| `ConZF/Mat.lean` | the recursion `F` (an `Acc.rec` on paths), coherent assignments, `materialize` (the value), `Desc`, `acc_of_desc` |
| `ConZF/Repl.lean` | the glued tree, `replacement` from the accessibility of the root |
| `ConZF/Rule.lean` | assignments generated by a one-node rule |
| `ConZF/Pair.lean`, `Ord.lean`, `VLevel.lean` | pairs, ordinals (trichotomy without EM), rank, levels, ω, the label set `D`; the class `Budget` of the properties of `D` used by the rule and the model, with the standard label set as instance |
| `ConZF/Reach.lean` | the uniform definability rule, over any budget |
| `ConZF/Fml.lean`, `Proof.lean`, `ZF.lean` | formulas, stable satisfaction, the proof system, soundness without EM, the axioms of ZF |
| `ConZF/ModelBase.lean` | first-order definability `ISat`, the class `HG`, closure of the hereditarily good ordinals |
| `ConZF/CofinalCut.lean` | cofinal cuts of a definable rank family; hereditary goodness of every cut; Replacement from a bound; `RankBounded` |
| `ConZF/Pointwise.lean` | Replacement from pointwise accessibility, by separately guarded evaluations |
| `ConZF/Model.lean` | `BoundHyp`, `hg_model_of_bounds`, `con_ZF_of_bounds`; the producers `AccHyp` and `PointwiseHGAcc`; `con_ZF`; all over any budget |
| `ConZF/Seeded.lean` | the seeded budget around a supplied set `b`, identity-source reachability of every ordinal up to `rank b`, and the seeded model: under `AccHyp` (hence under irrefutable EM) an explicit transitive class model of `ZF` containing `b` |
| `ConZF/AllGood.lean` | under EM, an interpreter for which every ordinal is hereditarily good gives an actual ordinal term bounding any functional ordinal-valued relation on a supplied source (the value of `F` at the glued root) |
| `ConZF/Lift.lean` | the adjacent-universe lift: preserves and reflects bisimulation, membership, ordinals; commutes with successor, powerset, rank, levels; `shrink` (lower representatives of upper subsets of lifts); `U` = all lifts, `K = rank U`, `U = V_K`, members of `K` are the lifted lower ordinals |
| `ConZF/MixedHartogs.lean` | under EM, an upper ordinal with a relational set-coded injection (`IsInjRel`; ordinary injections via `IsInjFun.toRel`) into `lift a` is below `lift (wfBound a.Idx)`; `K` is an initial ordinal |
| `ConZF/Cofinality.lean` | explicit cardinal notions relative to a class (`RegularIn`, `StrongLimitIn`, `InitialIn`, `InaccIn`); the upper-model interface `UpperModel` and the domain-coded cofinality interpreter over partial cofinal relations (`Cof`): extensional, unbounded on any domain with a cofinal relation in `N`, reaching every nonzero lower ordinal not inaccessible in `N` (three cases, the third by shrinking an internal powerset); with no smaller inaccessible, every lower ordinal is hereditarily good and every functional ordinal relation has a native bound |
| `ConZF/Interval.lean` | con23: seeded interval reachability (identity graphs below the seed, the cofinality cases above), the interval bound; `K` is initial and a strong limit against upper injections, regular against upper functions under the interval hypothesis; the interval theorem: under EM, for every seed `b`, `N` has an inaccessible in `(lift (rank b), K]`, the remaining premise being the interface `M` |
| `ConZF/Cardinal.lean` | the first-order cardinal interface: formulas for pairs, functions, injections, ordinals, internal powersets, regularity, initiality, strong limit, strong inaccessibility, and choice; satisfaction bridges over a transitive pair-closed class; `InaccIn N κ → N ⊨ "κ inaccessible"`; the theory `ZFCI`; conditional consistency of `ZFC + ∃ inaccessible` with explicit premises |
| `ConZF/Relativize.lean`, `InternalZF.lean` | conzf15 gate 1: uniform relativization of native formulas to a class defined by a unary formula; the stable transitive syntactic-ZF interface `SynZF` and its derived internal constructions from syntactic validity alone (separation by native formulas, pairs, unions, internal powersets and products, collection), graph normalization (cleaning functions and cofinal relations to pure ones in the class), and choice functions from the choice sentence |
| `ConZF/OmegaRec.lean` | internal ω-recursion as a native formula: partial sequences (pure set-coded graphs on numerals with the recursion equations) exist for every numeral and agree where both are defined; the sequence specified by `seqF step` exists in the class, is unique, and satisfies the recursion equations |
| `ConZF/Reflection.lean` | the finite-fragment reflection compiler (conzf16): computed free-variable bound, counterexample requests of the raw universal nodes (all valid), capture formulas, the closure conjunction, good-stage and least-next-stage formulas, set relativization and the reflection sentence, with all scope lemmas (`Bound 2 nextF`, `Bound 1 reflectionAt`) and the exact capture semantics |
| `ConZF/Assign.lean` | finite assignments as sets (gate 2, step A): pure set-coded functions from a numeral into a domain set, the formula and its bridge, the internal set of all assignments into a member, cons of a value with its formula and bridge, existence of the cons in the class, value reading, native packaging and negative reconstruction |
| `ConZF/Codes.lean`, `Step.lean` | gate 2, checkpoint 2: numeral and code-shape formulas for `Fml.enc` with exact bridges; the guarded validity/truth step as a native formula (new scope table from the old one, new truth table from the new scope table and the old truth table), its ambient reading, bridges, functionality, and totality in the class |
| `ConZF/History.lean` | gate 2, checkpoint 3: the totalized step, the validity/truth history as the ω-recursion sequence in the class, its rows, and the invariants by induction on the stage: a row `⟨enc φ, n̄⟩` is scoped exactly when `ht φ ≤ d` and `Bound n φ`, every scoped row decodes to a native formula, and a row `⟨enc φ, pack n e⟩` is true exactly when scoped and `φ` holds in the set structure at `e` |
| `ConZF/SetSat.lean` | gate 2, checkpoint 4: `ω`, the assignment set, and the start state as formulas; the set-satisfaction formula `setSatF` quantifying over the history; uniform correctness: a `SynZF` class satisfies it at `(A, enc φ, pack n e)` exactly when `Bound n φ` and `φ` holds in the set structure `A` at `e`; absoluteness between such classes |
| `ConZF/ScopedCode.lean`, `DefAdapter.lean`, `Def.lean` | gate 3: the scoped-code formula reading the scope component of the history with exact readback for arbitrary codes; the generic two-Separation `Def` adapter (existence and uniqueness for any evaluator and guard of the stated bounds, model-relative membership law); the instantiation `defPowF` with `Bound 2`, the native `defPow A = sep (Definable A) (powerset A)` with its exact membership, internal closure, congruence, absoluteness, `defPow ∅ ≈ {∅}`, `A ∈ defPow A`, and transitivity for transitive `A` |
| `ConZF/LHier.lean` | gate 4: relational level histories: the history formula (`Bound 2`, pure total functional graph on `succ α` satisfying the uniform recurrence `h(β) = ⋃_{γ ∈ β} defPow (h γ)`) with its ambient reading; any two histories agree on their common rows (overlap compatibility); histories exist in the class for every internal ordinal (∈-induction through collection) and are unique; the level relation `Level M α A` with existence, uniqueness, congruence, and restriction; the exact laws `L_0 = ∅`, `L_{succ β} = defPow L_β`, `L_λ = ⋃_{β∈λ} L_β` for limits, transitivity and monotonicity of levels (both from transitivity of `defPow` on transitive sets, not from monotonicity of `defPow`) |
| `ConZF/Constr.lean` | the guarded interface and constructibility: `ordLhistF`, `ordLevelF` (`Bound 2`) conjoining the ordinal formula, with readings carrying `IsOrd`; the ordinal formula exact over any transitive set structure without pair closure; ordinal identification `IsOrd ξ → (ξ ∈ L_α ↔ ξ ∈ α)`, hence `α ∈ L_{succ α}` and `L_α ∈ L_{succ α}`; level absoluteness between `SynZF` classes containing the index; the constructibility formula `constrF` (`Bound 1`) with exact membership, and the class `Constr M`: stable, respects `≈`, transitive, inside `M`, containing every ordinal of `M` and every level |
| `ConZF/ConstrAx.lean` | the constructible class `N := Constr M` as a model of every axiom of `ZF` other than Separation, under `SynZF M`: `Constr M` is the class defined by `constrF`; the least-stage formula (functional by ordinal comparison, total by `∈`-minimality); the level bound (a member of `M` with constructible elements lies inside one level, by Collection in `M` on least stages); pairing, union, and Power Set in `N` by definability over a bounding level, with the exact internal powerset law; Collection in `N` for native formulas by relativization, Collection in `M`, Separation of the constructible outputs, and the bounding level as witness; `valid_nonsep`, and `valid_of_sep` (with Separation, every axiom) |
| `ConZF/ReflectN.lean` | the semantic half of the reflection compiler for `N`: iterated quantifiers as finite environment prefixes; parameters read from assignment packages; the least-witness-stage formula (functional by ordinal comparison, total for active tuples by `∈`-minimality); bounded capture of one request over all parameter tuples from a set (Collection in `M` over the assignment set); the exact readings of `capture`, `closes`, `goodF`, `nextF`; existence and uniqueness of the next good stage; the totalized step and the ω-history through the internal ω-recursion (pure graph, ordinal orbit, strict growth, rows included along the index order); the supremum as a nonzero limit with the rows cofinal in it and finite fit of tuples into one row level; reflection at the supremum by induction on the formula with counterexample capture; Separation in `N` as a definable subset of the reflecting level; **`SynZF (Constr M)`** |
| `ConZF/UpperN.lean` | the concrete constructible upper class and the `UpperModel` assembly: a stable, bisimulation-respecting `ZFModel` is a `SynZF` class; at the upper universe the seeded class with seed `K` under the accessibility hypothesis, and its constructible class `NL` (a `SynZF` class containing `K`, every lifted lower ordinal, and identity graphs on its members); for any `SynZF` class, regularity implies initiality and limitness, and the three classification fields (identity graph from `ω`; smaller cofinality or regular by ordinal comparison; regular non-inaccessible gives a cofinal graph from the supplied family of subsets) without choice or a canonical order; the constructor `upperModel_of_leastCof` whose only inputs are the least-cofinal-relation interface, and `con_ZFCI_of_leastCof` with every outstanding assumption visible: excluded middle, the least partial cofinal relation with its laws, and choice in `NL` |
| `ConZF/Words.lean` | ordinal words (pure finite functions from a numeral into ordinals) with shortlex, linear up to bisimulation; the minimum principle (`minWord`): every inhabited stable class of words of a `SynZF` class has a least member, by minimizing the successful length below a supplied witness and then extending a prefix coordinate by coordinate, each minimization bounded by a supplied candidate; uniqueness; the formulas `owordF`, `wltF` with exact readings |
| `ConZF/Programs.lean` | finite definition programs as ordinal words evaluated by a stack machine (opcodes below `9`, other ordinals pushed as values; codes of formulas, `∅`, successor, cons of packages, and the definition instruction reading the level, code, and package through the guarded `Def` adapter); stacks, steps, runs, and denotations as pure finite functions with fixed native formulas and exact readings; functionality of the denotation; concatenation without pointer shifts; **coverage**: every constructible set of the class is the denotation of a program of the class |
| `ConZF/ChoiceN.lean` | the constructible class is its own constructible universe; the least cofinal graph as the denotation of the shortlex-least successful program (`LeastCofN`, with the four laws); the selector `Pick` as a fixed formula and the choice graph as one Separation of `a × ⋃a` (`valid_ac`); `upperModel_NL` with no remaining input, and **`con_ZFCI_of_em`: the consistency of `ZFC + ∃ inaccessible` from excluded middle alone** |
| `ConZF/Endpoint.lean` (with the additions to `MixedHartogs.lean`, `Interval.lean`, `Cardinal.lean`) | the pointwise endpoint (con33): the Hartogs pullback from accessibility of the source ordinal, so negative accessibility of `K` in the upper universe (`NNAccK`) gives non-injectability of `K`, initiality, and strong limitness by comparison and restriction; lower accessibility through the rank map, the interval bound as the rank of the exact image of the definability rule's Replacement, interval regularity, and the EM-free interval theorem with the final split by stability; `con_ZFCI_of_nnacc`; and **`con_ZFCI_of_pointwise`: the consistency of `ZFC + ∃ inaccessible` from pointwise accessibility of the hereditarily good ordinals of the seeded upper budget**, alternatively from the upper accessibility hypothesis alone (`con_ZFCI_of_upper_accHyp`) or from a bound producer for the seeded upper budget together with negative accessibility of `K` (`con_ZFCI_of_bounds_nnacc`); corollaries from double-negated upper membership well-foundedness, irrefutable excluded middle, and excluded middle |
| `ConZF/ProofCode.lean` | the arithmetic endpoint: a division-free Cantor pairing with a searched inverse, codes of formulas and of proof trees with fuelled decoders and exact round trips, the structural conclusion checker with explicit schema-instance tags, and the recursive Boolean checker `checkZF`; `prf_fls_iff_code : Prf ZF ⊥ ↔ ∃ n, checkZF n = true` and `con_iff_checker : Con ZF ↔ ∀ n, checkZF n = false`, a `Π⁰₁` sentence, with completeness using only propositional existence of a code |
| `ConZF/OrdDecode.lean`, `ConZF/DecodeSpectrum.lean`, `ConZF/DecoderCap.lean`, `ConZF/DecodeGraph.lean` | the decoder-capacity tranche (con46–47): the Σ₁ ordinal decoder `decodeF` (a diagram `⟨A, r⟩` is order-isomorphic to the ordinal `α` by an onto pure map; one unbounded existential, all others membership-bounded via the certificate `BForm`, `Bound 2` by `decide`), its exact class-guarded reading in any transitive class (`decode_read`), uniqueness of the ordinal and of the isomorphism graph, Σ₁ upward absoluteness; the exact ambient spectrum `decoded_spectrum` (ordinals with a pure injection into the base), ambient barriers as exact caps and as exact collecting sets, and **`barrier_iff_rankBounded`: for this decoder at an `HG` base, the rank-bound obligation of the model's Replacement clause is exactly negative existence of an `HG`-relative ordinary-injection barrier** (a reduction, not a producer); the controller cap `controller_le` (every ordinal embeddable in `U` has least controller `≤ rank (diagrams U)`, via downward absoluteness of the decoder at successor-closed rank stages and the parameter-free `maxOrdF` at successors), one native library `D (succ (λ U))` with a fixed small label type covering all children cofinally, and `HG` saturation (`injectable_hg`: embeddable ordinals of an `HG` base are `HG`, so `HG` and ambient spectra and barriers coincide there, `rankBounded_iff_ambient_barrier`); the graph checkpoint: a fixed small-carrier graph of the decoder's assignment whose *rank* has exactly the original decoded ordinals as members (`graphNode_exact`, `original_mem_graphHeight`), with `SWF` only, and `collecting_of_acc` recording what native accessibility would add. Open: any common bound on the decoded ordinals (including at `U = ω`), arbitrary-formula completeness, and the extension from the supplied base to arbitrary bases |
| `ConZF/Majorant.lean` | the native majorant compiler (con42): certificates `Plan` whose interpretation is an actual formula (input/parameter copy, element, subset, internal powerset, union, composition and image capturing the input, guard by an arbitrary native test, disjunction) and whose envelope `Plan.bound` is a native set; `Plan.sound` in any transitive class with no functionality, closure, accessibility or model premise; `Certificate ψ` with a Lean-checked equation to the original matrix; `isat_original_rank_bound`, a native ordinal bounding all output ranks of `ISat` uniformly in the stage; `certifiedRankBounded` and `certifiedReplacement` through the unchanged `HG` consumers; and the checked nested example `stress T = Image (Image (Guard (Compose Power Power) T))` for an arbitrary test `T`, with envelope `𝒫⁵ (⋃³ s)` (`stress_envelope`) and its `HG` Replacement instance `stress_replacement` needing only `HG` of source and parameters. An unconditional producer for the certified fragment only; it does not touch the decoder barrier |
| `ConZF/Acc.lean` | converse to `accHyp_of_mem_wf`; constructive accessibility lemmas |
| `ConZF/CanonicalAcc.lean` | the unpruned rule trees simulate every ordinal-membership descent |
| `ConZF/Markov.lean` | Markov witnesses from accessibility of omega; double-negation shift from `AccHyp` |
| `ConZF/Graph/Recursion.lean` | predecessor-local recursion into stable predicates from stable well-founded induction; the joined table of partial solutions |
| `ConZF/Graph/GSet.lean`, `Ops.lean`, `Rank.lean` | the graph route, experimental: graph sets with an `SWF` proof, bisimulation, membership, extensionality, membership induction; ranges, Separation, powerset; rank as transitive closure; the universal bound on a fixed carrier |
| `ConZF/Graph/Faithful.lean`, `Sat.lean`, `Def.lean` | faithful collapse of an extensional stable relation; satisfaction of `Fml` over graph sets; the definable powerset with its exact membership law |
| `ConZF/Graph/Ord.lean`, `Names.lean`, `Hier.lean`, `Levels.lean` | graph ordinals and normalization; names over an ordinal presentation, the earlier-stage graphs, locality, the solved table; the meaning of a name and the exact recurrence `L a ≈ ⋃_{b<a} Def (L b)`; monotone, transitive, presentation-invariant levels |
| `ConZF/Graph/Ordinals.lean` | the ordinal formula is correct over transitive graph sets; the ordinals of `L a` are the rank at `a`, hence `Ord ∩ L_α = α`; the canonical names of the ordinal and of the level at each stage |
| `ConZF/Graph/Pair.lean` | pairs, ordered pairs with injectivity, products, unions, and the reachable support of a graph set |
| `ConZF/Graph/Transport.lean`, `Monotone.lean` | finite-name transport along a bisimulation; bounded inductive definitions on the points of a graph set: the history by predicate recursion, persistence after stationarity, Hartogs-forced convergence, the native stopping cut and (for monotone operators) least closed endpoint; the packaged certificate `⟨γ, ⟨σ, C⟩⟩` in the uniform envelope `κ × (P(κ × P X) × P X)`, first-entry ranks and the ranked certificate; the adjoin-a-set instance and the predecessor-rule benchmark on an ordinal `η`, whose stopping cut is `η` |
| `ConZF/Graph/Normalize.lean` | con20 normalization: well-founded justification certificates over a supplied judgement set and set-coded rules exist (negatively) exactly when the judgement is in the least closed set; the normalized certificate `(W, ρ-decreasing dependencies, id, q)` lies in an envelope depending only on the judgement set, and the menu over `W` is complete |
| `ConZF/Graph/Hartogs.lean` | relational pullback: the rooted graph of the representatives of a transitive domain is that domain, exactly; the relational Hartogs bound, an ordinal with no injection relation into its argument, extensional; the source-wide menu |
| `ConZF/Ideal.lean` | ideal sets (¬¬-sheafification of the setoid of sets, without higher inductive types); unique choice into ideals; the parent constructor is a small-indexed Collection |
| `ConZF/NativeBound.lean` | unconditional bounds from a small type of certificates; well-founded codes and their heights; `ω` as a test |
| `ConZF/Sim.lean` | negative upper simulations: certificates that bound a target without presenting it; the enclosing tree of all codes on a carrier; the diagonal fact |
| `ConZF/Cover.lean` | native covers: small candidate families over literal inputs give `RankBounded`; existential composition by dependent sums |
| `ConZF/Envelope.lean` | least-rank witness envelopes and their capped approximations; the exact criterion; a cap meeting each nonempty witness class gives `RankBounded` |
| `ConZF/RankGraph.lean`, `RankFml.lean` | rank is first-order over `HG`: native transitive closure and rank graph, their `HG` bounds, uniqueness of rank assignments, the formula `rankFml` and its readback |
| `ConZF/EnvFml.lean` | the envelope as a formula; envelope instances are total and functional; `EnvBoundHyp → BoundHyp`, the normal-form reduction |
| `ConZF/Bar.lean` | stable binary bar induction on the negative Cantor space: the greedy real by Separation; local caps join; the single-critical-real criterion |
| `ConZF/FinObs.lean` | a producer: total finite-observation specifications on the negative Cantor space have a source-wide cap and a witness menu; `RankBounded` for such formulas |
| `ConZF/CountableSource.lean` | the scope check: a countable source lifts to the Cantor space with a local cap at every nonzero point, and its cap is exactly a local cap at the zero real |
| `ConZF/FinPred.lean` | a producer from semantic hypotheses: negatively finite predecessors give finite heights, simulations into `natCode`, and a collapse bounded by `ω` |
